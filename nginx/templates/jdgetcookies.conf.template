server {
    listen       ${PORT};
    listen  [::]:${PORT};
    server_name  ${HOST};
    
    location / {
       include /etc/nginx/uwsgi_params;
       uwsgi_pass http://${APP_CONTAINER_NAME}:${APP_CONTAINER_PORT};
     }
}
